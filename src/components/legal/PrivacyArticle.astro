---
import { getFixedT, type Locale } from '../../i18n/i18n';
import { siteConfig } from '../../site.config';
import { homePathForLocale, pageHref } from '../../lib/paths';

interface Props {
	locale: Locale;
}

const { locale } = Astro.props;
const t = getFixedT(locale);
const homeUrl = pageHref(homePathForLocale(locale));
const email = siteConfig.contact.email;
const phone = siteConfig.contact.phone;
const tel = phone.replace(/\s/g, '');
const phoneLink = `<a href="tel:${tel}" class="text-red-armenian hover:underline">${phone}</a>`;
---

<main class="min-h-screen">
	<article class="container mx-auto max-w-3xl px-4 py-12 md:py-16">
		<header class="mb-10">
			<a href={homeUrl} class="text-red-armenian hover:text-blue-armenian text-sm font-medium mb-4 inline-block">
				{t('Privacy.back')}
			</a>
			<h1 class="text-3xl md:text-4xl font-bold text-gray-900">{t('Privacy.h1')}</h1>
			<p class="text-gray-500 mt-2">{t('Privacy.updated')}</p>
		</header>

		<div class="prose prose-gray max-w-none space-y-8 text-gray-700">
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s1h')}</h2>
				<p
					set:html={
						t('Privacy.s1p1') +
						' ' +
						phoneLink +
						' ' +
						t('Privacy.s1p1After', { email })
					}
				/>
				<p>{t('Privacy.s1p2')}</p>
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s2h')}</h2>
				<p>{t('Privacy.s2p1')}</p>
				<p>{t('Privacy.s2p2')}</p>
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s3h')}</h2>
				<p>{t('Privacy.s3p1')}</p>
				<p>{t('Privacy.s3p2')}</p>
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s4h')}</h2>
				<p>{t('Privacy.s4p1')}</p>
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s5h')}</h2>
				<p>{t('Privacy.s5p1')}</p>
				<p>{t('Privacy.s5p2')}</p>
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s6h')}</h2>
				<p>
					{t('Privacy.s6p1', { phone })}
					<span set:html={t('Privacy.s6p2', { email })} />
				</p>
				<p set:html={t('Privacy.s6p3')} />
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s7h')}</h2>
				<p>{t('Privacy.s7p1')}</p>
				<p>{t('Privacy.s7p2')}</p>
			</section>
			<section>
				<h2 class="text-xl font-semibold text-gray-900 mb-2">{t('Privacy.s8h')}</h2>
				<p>{t('Privacy.s8p1')}</p>
			</section>
		</div>
	</article>
</main>
